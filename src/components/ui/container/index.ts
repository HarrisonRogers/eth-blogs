import Container from './container';

export {
  default as containerVariants,
  type ContainerVariantProps,
} from './variants';
export { type ContainerProps } from './container';

export default Container;
